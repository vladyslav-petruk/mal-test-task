import { Text, View } from 'react-native';

/** User summary + onboarding entry — wire /me + draft in milestone 1. */
export function HomeScreen() {
  return (
    <View>
      <Text>Home</Text>
    </View>
  );
}
