import { Text, View } from 'react-native';

/** Single screen with internal stepper (5 steps + review/submit). */
export function OnboardingScreen() {
  return (
    <View>
      <Text>Onboarding</Text>
    </View>
  );
}
